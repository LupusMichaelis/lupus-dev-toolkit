### Subsystem  igor ############################################################
FROM lupusmichaelis/alpine:1.0.1 AS igor
LABEL description="Alpine Linux with dev user"

ARG USER_ALIAS
ARG UID
ARG GID

ENV USER_ALIAS ${USER_ALIAS}
ENV UID ${UID}
ENV GID ${GID}
