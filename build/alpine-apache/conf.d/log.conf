# In a container, redirect everything to std outputs
# We need to do that from the parent process or output from children will be ignored

CustomLog /dev/fd/1 common
ErrorLog /dev/fd/2

# vim: set ft=apache noet :
